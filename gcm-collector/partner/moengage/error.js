const { consoleLog } = require("../../utils/tools")

async function handleErrorForMoengage(exception) {
    consoleLog("TODO return failure error code callback for moengage")
}

module.exports = { handleErrorForMoengage}